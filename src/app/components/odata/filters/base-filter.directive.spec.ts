import { BaseFilterDirective } from './base-filter.directive';

describe('BaseFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseFilterDirective();
    expect(directive).toBeTruthy();
  });
});
